1. First think i learned about the nodemon.

``` 
    npm install -g nodemon 

```
which is useful for running my express code into the terminal without refreshing the windows again and again.

2. `req` and `res`

3. `send` and `listen`

4. `chunk`